# このディレクトリ(config.php・SQLite・ログ)をWebから直接アクセスさせない多層防御。
# 本来はpublic_htmlの外側に配置するため到達しないはずだが、誤配置時の保険として設置する。

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
