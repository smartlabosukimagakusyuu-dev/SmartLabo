# このディレクトリ（設定ファイル・送信間隔制限の記録）はWebから一切見せない。
# 本来はドキュメントルートの外へ置くため到達しないが、誤配置時の保険として設置する。

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
