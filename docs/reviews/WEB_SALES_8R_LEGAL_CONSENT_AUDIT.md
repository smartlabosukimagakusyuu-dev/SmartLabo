# WEB-SALES-8R 法務文書・申込み前確認・同意記録の独立監査 記録

**実施日**：2026-08-11
**対象**：`feature/web-sales-8-legal-consent`（HEAD `824daa3`）
**性質**：読み取り・検査のみ。製品変更0件
**判定**：**No-Go**（重大5件・中4件・軽微7件）

> 本記録は監査結果の保存用である。是正は WEB-SALES-8F（`WEB_SALES_8F_LEGAL_FIXES.md`）で行った。
> 監査は法的評価を行っていない。以下はすべて**実装事実の実測**である。

---

## 1. 監査時のGit状態（14項目すべて一致）

| 項目 | 実測 |
| --- | --- |
| 製品branch / HEAD | `feature/web-sales-8-legal-consent` / `824daa3` |
| local feature == origin feature | 一致 |
| working tree | clean（untracked 0 / stash 0） |
| master | `ec88585`（不変） |
| WEB-SALES-8 commit | featureに含まれる（master..HEAD は1件） |
| feature差分 | master未統合 |
| migration | 001〜024 |
| テストファイル | 20本 |
| テスト結果 | 2436 PASS / 0 FAIL / 1 skip（ファイル別も報告と一致） |
| `.env` | Git追跡外 |
| 本番稼働版 | `d8c1558`（masterの祖先） |
| 自動デプロイ機構 | 0件 |

---

## 2. 差異一覧

### 重大（No-Go要因）5件

| ID | 内容 | 実測 |
| --- | --- | --- |
| **R1** | **解約期限が実装されていない** | 文書・画面は「更新日の前日まで」。`scheduleCancellation` に期限判定が一切なく、期間終了60秒前でも **HTTP 200 で受理**。repo全体で締切の実装・定数・比較が0件 |
| **R2** | **消費税がStripeへ一切送られない** | Checkout Session の全パラメータを実測。`automatic_tax` / `tax_rates` / `default_tax_rates` いずれも送信なし。repo全体で税設定コードが0件。表示「税別・別途申し受けます」と実請求が食い違う |
| **R3** | **過去版の本文を再現できないのに「再現可能」と記載** | 保存は version と digest(SHA-256) のみ。本文は現行ファイルにしかなく、上書きで失われる。version→commit の対応表なし。本文変更を検出するテストもなし（digest検証が同語反復） |
| **R4** | **法務ページURLに X-Robots-Tag が付かない** | Nodeは静的配信を持たず（`express.static`/`sendFile` が0件）、`/legal/terms` 等は404。ヘッダーが付くのは `/api/legal/*` のJSONのみ。nginx設定は両repoに不在。完了報告の「二重に noindex」は3画面URLについて不成立 |
| **R5** | **表示した条件と記録・請求される条件がずれる** | 画面が3名26,000円を表示したまま人数が10名へ変わると、同意APIは**10名47,000円で記録**し、Checkoutまで200で通る（追加席 quantity=9 をStripeへ送信）。fail-open |

### 中 4件

| ID | 内容 |
| --- | --- |
| **M1** | 料金変更時に `PRICING_TERMS_VERSION` を上げ忘れると、その会社が**永久に申込不能**になる。再同意APIは200と「同意済み」を返すのに台帳は古いまま、Checkoutは409のまま（実測再現） |
| **M2** | 解約後90日の削除を実行する手段が製品にも運用手順にも存在しない。製品は論理削除のみ。FK（NO ACTION）が会社行削除を拒否する |
| **M3** | migration 024 に CHECK 制約が0件。負の人数・負の金額・不正形式のdigest・不正な日時がDB層で受理される |
| **M4** | company削除・user削除時の方針が未定義。FK制約により削除が拒否され、M2と衝突する |

### 軽微 7件

| ID | 内容 |
| --- | --- |
| L1 | `/api/legal/__proto__` 等が404にならず 200 `{"data":{}}` を返す |
| L2 | 法務APIに `Cache-Control` 未設定 |
| L3 | 法務ページに `canonical` 未設定 |
| L4 | `FinalConfirmation.jsx` にブラウザ側の単価リテラル `?? 3000` が残る |
| L5 | 同意APIに回数制限がなく、監査ログが連打で無制限に増える |
| L6 | `consentRepository.insert` の catch が全例外を握りつぶす |
| L7 | 完了報告の「前提の追加（5ファイル）」は実測6ファイル。「console error 0」は未ログイン初期表示の401を含めると厳密には0でない |

### 参考：公開Website（master × WEBSITE）— 本工程外

| ID | 内容 |
| --- | --- |
| W1 | `terms.html` / `privacy.html` に `[CEO確認待ち]`（管轄裁判所を含む）が残存 |
| W2 | 同2ファイルに「弁護士等の専門家による確認・最終調整を経てください」が公開本文として残存 |
| W3 | 公開Websiteに**特定商取引法に基づく表記のページが存在しない**（sitemapにも不在） |
| W4 | `terms.html` 自体が「ホームページの利用条件であり、サービス契約条件は別途」と明記しており、公開側にサービス規約が無いことを裏づけている |

---

## 3. 該当しなかった重大項目（実測で確認済み）

- 同意なしでStripeを呼べる → **該当なし**（409・Stripe 0回）
- 旧版・別人数・別料金の同意でCheckoutを開始できる → **該当なし**（fail-closed）
- 他社同意を利用できる → **該当なし**
- migrationが既存データを壊す → **該当なし**（024は非破壊）
- 秘密値・Stripe ID・URL・不要なPIIの露出 → **該当なし**
- 既存テストの削除・skip・緩和 → **該当なし**

実装の骨格（サーバー側強制・追記専用台帳・他社分離・改ざん耐性・テスト網羅性）は堅牢であり、
指摘は「文書の約束と実装の一致」「税」「証拠の再現性」「配信構造」に集中した。

---

## 4. 監査時点の判定

| 対象 | 判定 |
| --- | --- |
| WEB-SALES-8R | **No-Go**（R1・R2・R3・R4 が判定基準に該当） |
| master統合 | **No-Go**（R1・R3・R5・M1 はコード修正を要する） |
| 本番公開 | **No-Go** |

### 残存条件（監査時点）

1. 弁護士による最終確認が未完了（特商法の適用、AI送信の位置づけ、免責・管轄・規約変更条項、個人事業主の消費者性）
2. 本番Price作成と税設定が未完了
3. 本番Webhook未登録
4. 本番migration 017〜024 未適用
5. 公開Website是正が未完了（W1〜W4）
6. 解約後90日の削除運用手順が未整備
7. 代表による所在地・電話番号の目視照合（repo内に承認値の一次記録がないため監査では検証不能）
8. 本番配信構造（nginx）でのnoindexヘッダー確認

---

## 5. 監査の限界

- 本番VPS・本番DB・本番`.env`・Stripe実通信は一切行っていない
- 消費税の結論は「コードがStripeへ何を送っているかの実測」に基づく。本番Stripeアカウント／Priceの設定は未確認
- スクリーンショットは取得できていない（ブラウザペイン非表示のため）。画面確認はすべてDOM実測値
- 法的評価は行っていない
