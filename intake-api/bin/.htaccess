# HP Intake API — 管理CLI（bin/）。
# ★本番では **public_html の外**へ置く（SSOT v1.12 §10.11）。
#   この .htaccess は「誤って公開領域へ置いてしまった」ときの二重防御である。
# ★CLI 本体も PHP_SAPI が cli でなければ即終了する（多層防御）。
# ★Apache 2.4 の書き方。実コード・秘密値を含めない。
Require all denied
